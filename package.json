{
  "name": "salonos-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix SalonOS-main/frontend run dev",
    "build": "npm --prefix SalonOS-main/frontend install && npm --prefix SalonOS-main/frontend run build",
    "install:frontend": "npm --prefix SalonOS-main/frontend install",
    "postinstall": "npm --prefix SalonOS-main/frontend install"
  }
}
